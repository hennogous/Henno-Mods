# Optional Art Pack split test (20 September 2026)

Moved only `CSC_BAKERS_Wind_Mill_2` from core CSC into the new `CSC Art Pack` project. The shared Bakers' Stage 2 source property, Lua mirror, and GamePropertyRanges stay in core because Water Mill 2 still uses them. Art Pack adds one `BuildingVariants` child for the existing Bakers' district in a distinct ArtDef; runtime merge behavior remains to be tested in game.

The Art Pack needs `CSC Art Pack.Build.targets` to add sibling CSC as a cooker pantry. Without it, ModBuddy reports success but the cook drops the building ArtDef reference and cannot resolve shared materials/geometry. An isolated MSBuild cook with the target succeeded for Windows and Mac; the generated `.modinfo` contains CSC dependency and `UpdateArt`. Source XML and asset hash checks passed. No live game was running, so no runtime merge test was performed. See `project/docs/art/optional-art-pack-test.md` for the test procedure.

The starter `.civ6proj` was subsequently saved over the generated project wiring. Restored the `Content` items for ArtDef, AST and XLP, plus `None` items for the art manifest and build target, folders, dependency and `UpdateArt`. The isolated build was repeated and its generated `.modinfo` was checked. Reload the project in ModBuddy after outside edits so its project tree reads the current `.civ6proj`.

2026-09-20 follow-up: Shadow deleted `CSC Art Pack.Build.targets` and said it is not needed. Removed its stale project import and restored the AST Content entry. The standard isolated ModBuddy build exits successfully and resolves `(Mod Art Dependency File)` to `CSC_Art_Pack.dep` in `.modinfo`, but the cooker cannot find `CSC_Buildings.artdef`, two shared geos, or two shared materials and substitutes an error tilebase. The art manifest's Landmarks, WorldView_Translate, TileBase package, and CSC required-art ID match CSC's pattern; the build failure is a pantry/source dependency issue, not a consumer/library manifest issue. The civ6-art skill now documents the two separate UpdateArt dropdown entries and the `.dep` placeholder behavior. ModBuddy was open during edits and rewrote the project once; reload the project before further saves.

Correction after comparing main CSC's `.civ6proj`: source `.ast`, `.geo`, `.fgx`, `.mtl`, `.tex`, and `.dds` files do not need ModBuddy Content entries; main CSC has none and no targets file. The Art Pack AST entry was removed; its `.Art.xml` was made Content to match main CSC. An isolated build proved the cooker still discovers the AST, but cannot resolve source dependencies housed only under the sibling project. See `project/docs/art/optional-art-pack-test.md` for the precise source dependency list. The earlier assertion that the AST required a project entry was wrong.

After Shadow asked to copy dependencies and keep the Art Pack lean, copied the two required `.geo`/`.fgx` pairs, two `.mtl` files, and seven `.tex`/`.dds` pairs into the Art Pack. Copying the full `CSC_Buildings.artdef` would pull `CSC_Districts`, `CSC_StrategicView`, and `CSC_Landmarks` into the pack dependency graph, so those ArtDef copies were removed. The standard cook now builds the Wind Mill tilebase and texture data cleanly but defaults the cross-mod `Tag_HeroBuilding` reference. Added `project/tools/artpack/restore_wind_mill_artdef.ps1` to restore that one reference in the built ArtDef. Tested it on isolated build output; it restored the CSC building/path. Runtime cross-mod ArtDef resolution and district merge remain untested.

The post-build fix was moved into `CSC Art Pack/Finish ModBuddy Build.ps1` with a double-click `Finish ModBuddy Build.cmd` launcher. Both are `None` items in the ModBuddy project, so they are visible but not packaged. The launcher defaults to ModBuddy's normal built mod folder, accepts an optional folder argument, and rejects stale/error cooks missing the Bakers atlas. Tested it against the isolated valid output and confirmed it rejects the older installed output. The previous `project/tools/artpack` script path is obsolete.

Shadow subsequently reported "it works!" and asked to promote general ArtDef and Civ art lessons into the `civ6-art` skill. Updated `references/art-pipeline.md` with the additive Landmarks variant pattern, ModBuddy source discovery, cross-mod cooker pantry limits, selective post-build ArtDef reference restoration, and contextual `UpdateArt` guidance; corrected contradictory `references/art-troubleshooting.md` examples. The exact visual states Shadow checked were not specified in the conversation.

After Shadow recoloured and renamed the Art Pack atlases to `_2`, the installed ModBuddy build had zero-byte Windows/MacOS tilebase BLPs and no `SHARED_DATA` despite reporting build success. The new `_2` DDS files were full-mip; old Art Pack base DDS copies had only one mip, and the non-emissive material still referenced them. Renamed the Art Pack's E/NE materials to `CSC_BAKERS_E_2`/`CSC_BAKERS_NE_2`, updated the alternate AST references, and pointed both materials to `CSC_Atlas_B_BAKERS_2`, `CSC_Atlas_M_2`, and `CSC_Atlas_G_2`. Isolated cook then produced a nonzero BLP and the three new cooked textures. Updated `Finish ModBuddy Build.ps1` to check nonzero BLP plus the renamed textures; it accepts the valid isolated build and rejects the older broken installed output. Shadow needs to rebuild in ModBuddy, then run the CMD again.

20 September follow-up: Henno requested the entire alternate-art pipeline live in the optional Art Pack. Moved five gated property modifiers and six building attachments out of Bakers/Tailors SQL into `CSC Art Pack/Data/CSC_ArtPack_Properties.sql`; moved the shared Lua mirror and GamePropertyRanges ArtDef into the pack; moved five remaining expanded landmark variants, eight alternate-named ASTs, and three alternate-only prop ASTs. The pack XLP now has 12 entries including Wind Mill 2. Copied the local geometry/material/texture source closure needed to cook those tilebases. Removed the alternate variants, bridge actions/content, XLP entries, and exclusive geometry from CSC. The pack finishing script now restores six cross-mod Tag_HeroBuilding references. Isolated ModBuddy/MSBuild cooks for both projects succeeded; the Art Pack Windows tilebase BLP was 1,056,768 bytes; the post-build fix passed. SQLite syntax check produced five modifiers, ten arguments, and six attachments. Tailors contract/validator still codifies the former bridge ownership and needs deliberate rebaseline; do not treat its paths as current. Five newly transferred expanded variants still need in-game selection testing.
