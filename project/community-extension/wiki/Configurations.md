# Configurations

## CommunityExtension.ini

Some settings can be enabled by creating the `CommunityExtension.ini` file at /Base/Binaries/Win64Steam in your Civilization VI game files.

```ini
[Settings]
EnableConsole=1 ; Enables CE console. Useful for debugging.
```