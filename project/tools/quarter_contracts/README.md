# Quarter contract tools

Run the validator from the repository root:

```powershell
py -3 project/tools/quarter_contracts/validate_quarter.py tailors
```

See `project/docs/QUARTER-VALIDATION.md` for modes and interpretation.
